{% extends 'base.html.twig' %}


{% block title %}Destinations{% endblock %}

{% block body %}
<style>
    h1{
        text-align:center;
        font-size:50px;
        padding-top:10%;
        color:#8f8f8f;
    }

    .gestion_button{
        display:inline;
    }

    .gestion_button button{
        margin-right:1%;
        margin-bottom:1%;
    }
</style>

<div class="container">
    <h1>Destinations</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Ville</th>
                <td>{{ destination.ville }}</td>
            </tr>
            <tr>
                <th>Latitude</th>
                <td>{{ destination.lat }}</td>
            </tr>
            <tr>
                <th>Longitude</th>
                <td>{{ destination.lng }}</td>
            </tr>
            <tr>
                <th>Créer le</th>
                <td>{{ destination.createdAt ? destination.createdAt|date('d-m-Y') : '' }}</td>
            </tr>
        </tbody>
    </table>

<div class= "gestion_button">
    <button class= "btn btn-primary"> <a href="{{ path('admin_destinations_index') }}">back to list</a></button>

    <button class= "btn btn-primary"> <a href="{{ path('admin_destinations_edit', {'id': destination.id}) }}">edit</a></button>

    {{ include('admin/destinations/_delete_form.html.twig') }}
</div>
</div>
{% endblock %}
